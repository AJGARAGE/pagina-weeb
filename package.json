{
  "name": "AGARAGE-pagina-weeb",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node servidor/server.js",
    "build-cliente": "npm install --prefix cliente && npm run build --prefix cliente",
    "postinstall": "npm install --prefix cliente && npm install --prefix servidor"
  }
}
